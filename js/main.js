document.getElementById('loginForm').addEventListener('submit', function (e) {
  e.preventDefault();
  // Simulación de login exitoso
  window.location.href = "dashboard.html"; // redirige al panel principal (por crear)
});

